[project]
name = "finance-ticket-agent-api"
version = "0.1.0"
description = "FastAPI service for a finance ticket Agent MVP"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "langgraph",
  "uvicorn"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
