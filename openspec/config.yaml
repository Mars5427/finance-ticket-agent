schema: spec-driven

context: |
  Project: 面向财务场景的智能工单处理 Agent.
  Resume stack: Python, FastAPI, LangGraph, React, TypeScript, Go, PostgreSQL, Redis, RAG, Tool Calling, Docker, OpenSpec.
  Scope: only reimbursement policy Q&A, balance anomaly explanation, and reconciliation anomaly localization.
  Boundary: the Agent never connects directly to the Go service PostgreSQL database; it uses MCP-style Tool Calling wrappers over Go HTTP APIs.
  Documentation language: Chinese for README and docs, with concise English identifiers in code/specs.

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
